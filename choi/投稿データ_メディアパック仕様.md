# ちょい英語ミニブック投稿システム — 投稿データ / メディアパック仕様

本書は、ユニット投稿(UC-20)および投稿データ更新(UC-22)で扱う、
投稿データ(エクセル) / 音声パック / 画像パック の構造と処理仕様を定める。

## 0. 関連仕様の前提

本書を読む前に、ユースケース仕様書(`ちょい英語ミニブック投稿システム_
ユースケース_画面一覧.md`)の以下のセクションを既知とする:

- 「教材階層のIDの二系統(公開ID と 編集ID)」
- 「S3 データの公開範囲(フェーズ別方針)」
- 「3. ユニット投稿」セクション冒頭の投稿フロー基本方針
  (「**本登録 → 自動生成 → A-MU2 でレビュー(必要なら修正)**」、登録前
  プレビュー画面は提供しない)

---

## 1. エクセル構造

### 1.1 シート構成

- シート数: 1(シート名は任意、実装上は先頭シートのみを読む)
- 1行目: ヘッダー行
- 2行目以降: データ行(1行=1センテンス)

### 1.2 列定義(17列)

エクセルは **編集ID 4列** + **表示用 11列** + **拡張 2列** = **計17列** を
最大構成とする。
編集ID列は内部処理用で、投稿者は触らない(後述 §1.4)。

**新規作成時(UC-20)は編集ID列(列1〜4)を省略してよい**。新規作成用の
投稿者向け標準テンプレートは表示用列のみで構成し、編集ID列は含めなくてよい。
編集ID列が揃う形が現れるのは、UC-23 でダウンロードしたラウンドトリップ用
エクセル(更新 UC-22 で再アップロードするもの)に限る。下表はその全列形の
定義(§1.4 / §4.1)。

| # | 列名 | 対応する概念モデル属性 | 記入粒度 | 必須 | 区分 |
|---|---|---|---|---|---|
| 1 | パート編集ID | パート.編集ID | パートの**先頭行のみ**記入 | システム | 編集ID |
| 2 | デッキ編集ID | デッキ.編集ID | デッキの**先頭行のみ**記入 | システム | 編集ID |
| 3 | カード編集ID | カード.編集ID | カードの**先頭行のみ**記入 | システム | 編集ID |
| 4 | センテンス編集ID | センテンス.編集ID | **各行に記入**(センテンスは行と1対1) | システム | 編集ID |
| 5 | パート | パート.タイトル(パート識別キー兼用 §1.3) | 各パートの全行に同じ値 | 任意 | 表示用 |
| 6 | チャプター名 | デッキ.タイトル(デッキ識別キー兼用 §1.3) | 各デッキの全行に同じ値 | 必須 | 表示用 |
| 7 | シーン名 | カード.タイトル(カード識別キー兼用 §1.3) | 各カードの全行に同じ値 | 必須 | 表示用 |
| 8 | 話者 | センテンス.話者 | 各行固有 | 任意 | 表示用 |
| 9 | 英会話 | センテンス.英文 | 各行固有 | 必須 | 表示用 |
| 10 | 手順3で作成した語順訳 | センテンス.直訳 | 各行固有 | 任意 | 表示用 |
| 11 | 和訳 | センテンス.和訳 | 各行固有 | 任意 | 表示用 |
| 12 | チェック | 投稿者側の作業フラグ(DBには保存しない) | 各行固有 | 任意 | 表示用 |
| 13 | 音声ファイル | センテンス.音声ファイルID に相当 | 各行に記入(1発話=1音声) | 条件付き | 表示用 |
| 14 | デッキ画像 | デッキ.画像ファイルID に相当 | **デッキの先頭行のみ**記入、他行は空欄 | 条件付き | 表示用 |
| 15 | カード画像 | カード.画像ファイルID に相当 | **カードの先頭行のみ**記入、他行は空欄 | 条件付き | 表示用 |
| 16 | カード属性 | カード.拡張属性 | **カードの先頭行のみ**記入、他行は空欄 | 任意 | 拡張 |
| 17 | デッキ属性 | デッキ.拡張属性 | **デッキの先頭行のみ**記入、他行は空欄 | 任意 | 拡張 |

列16「カード属性」・列17「デッキ属性」は、教材ファミリー固有のメタデータを
運ぶための**拡張属性列**である。値は JSON としてパースできる文字列であり、
**システムは中身を解釈しない**(規定の全体は §1.7、出力先は §9.1 / §9.2)。
両列とも省略可(列ごと無くてよい)。

**列の同定はヘッダー名で行う**。本表の列番号は標準テンプレートおよび
UC-23 ダウンロード出力の標準列順を定めるものであり(ただし列12
「チェック」は DB 非保存のため DL 出力に含まれない — §7 手順2)、
アップロード時の列の解釈は位置ではなくヘッダー名による(既知の列名に
一致しないヘッダーの列は読み飛ばされる)。したがって任意列は個別に
省略でき、例えば列16「カード属性」のみを省略して列17「デッキ属性」を
使うこともできる。

列5「パート」は**省略可**。パートを使わないユニット(ミニブック等)では、
この列ごと省略してよい(デッキはブック直下に帰属)。列がある場合、パート名の
値が変わる境界で新しいパートが始まる(§1.3)。パート列を省略したときの
編集ID列の扱いは §1.4 を参照。

列13〜15 の「条件付き」は、TTS/AI画像の自動生成が ON の場合は空欄可、
OFF の場合は必須となる(詳細は §3)。

列8「話者」は、TTS自動生成モードではボイス解決のキーを兼ねる
(解決ルールは §3.3「TTSボイスの解決」を参照)。

### 1.3 階層の識別ルール

- **パートの識別**: 「パート」列がある場合、値が変わる境界で新しいパートが
  始まる。「パート」列が無い(省略)場合、そのユニットはパートを持たず、
  デッキはブック直下に帰属する
- **デッキの識別**: 「チャプター名」の値が変わる境界で新しいデッキが始まる
- **カードの識別**: 「シーン名」の値が変わる境界で新しいカードが始まる
- **センテンスの識別**: データ行1行 = 1センテンス
- **境界判定の方式**: 値の変わり目は **直前のデータ行との比較**(正規化後の
  値 §1.7 の、コードポイント単位の完全一致)で判定する。したがって同じ
  パート名/チャプター名/シーン名でも、**連続していなければ別の階層要素**に
  なる(例: チャプター名が A → B → A と並ぶと A のデッキが2つできる)
- **パート・デッキ・カードの表示順**: エクセル内での登場順をそのまま表示順とする
- **パートの実在**: パートはデッキのグルーピング層であり、デッキを1件も
  持たないパートは存在しない(概念モデル: パート → デッキ [0..1 -- 1..*])。
  差分更新(UC-22)の結果デッキを全て失ったパートは削除される
  (§7 末尾の注記参照)
- **混在構成の出力**: パート所属デッキとブック直下デッキが混在する状態
  (Web UI 上の操作で生じうる)は、パート列の有無がユニット単位で決まるため
  エクセル形式では表現できない。この状態のユニットをダウンロードする場合、
  直下デッキは**先頭の無題パート(パート名は空欄。リテラル「無題」は
  書き込まない)**として出力する。再アップロードすると
  直下デッキは当該無題パートに帰属する(非可逆な変換)。この変換について、
  ダウンロード時に A-MU2 上でガイドを表示する(§7 手順2)

### 1.4 編集IDの記入ルール

編集IDは、投稿データ更新時の create / update 判定および
階層構造の同定に用いる内部識別子である(詳細はユースケース仕様書
「教材階層のIDの二系統」参照)。

**記入ルール**:

- **パート編集ID / デッキ編集ID / カード編集ID**: その階層の
  **先頭行のみ**記入(他行は空欄)
- **センテンス編集ID**: **各行に記入**(センテンスはエクセルの行と
  1対1対応)

**初回投稿時(UC-20)**:

- 編集ID 4列は**省略してよい**(エクセルに含めない)。新規作成用の
  標準テンプレートは表示用列のみ
- システムが投稿成功時に編集IDを発行し、内部的に各レコードに付与

**パートを使わないユニット(ミニブック等)**:

- 列5「パート」を省略する。これに伴い、ラウンドトリップ用エクセルでも
  列1「パート編集ID」は現れない(パートが存在しないため)
- デッキはブック直下に帰属する(概念モデル: Unit → デッキ [1 -- 1..*])

**更新時(UC-22)**:

- UC-23 で DLされたエクセルには、各階層の編集IDが反映されている
  (パートを持つユニットはパート編集IDを含む)
- 投稿者はこのエクセルを編集してアップロードする
- システム側の判定:
  - 編集ID列が空欄の行 → **新規作成**(create)
  - 編集ID列に値がある行 → **更新**(update)
- 投稿者向けガイド: **「編集ID列は触らないでください」** とエクセル
  ヘッダのコメントおよびUI上のヘルプテキストで案内する

**先頭行ルール違反のバリデーション**:

- パート/デッキ/カードの編集ID列で、先頭行以外に値が入っていれば
  バリデーションエラー
- センテンス編集ID列は、空欄(create)または値あり(update)の
  どちらでも可

### 1.5 デッキ画像・カード画像の先頭行ルール

以下はパートを使わないユニット(ミニブック)の例。パートを使う場合は、
最前列に「パート編集ID」列が、表示用列の先頭(チャプター名の前)に「パート」列が
加わる(列順は §1.2 参照)。

```
デッキ編集ID | カード編集ID | センテンス編集ID | チャプター名 | シーン名 | ... | デッキ画像 | カード画像
d_001        | c_001        | s_0001           | チャプター1  | シーン1  | ... | c1.png    | c1s1.png     ← デッキ先頭 かつ カード先頭
             |              | s_0002           | チャプター1  | シーン1  | ... |           |              ← 2行目以降は空欄
             |              | s_0003           | チャプター1  | シーン1  | ... |           |              ← 同上
             |              | s_0004           | チャプター1  | シーン1  | ... |           |              ← 同上
             | c_002        | s_0005           | チャプター1  | シーン2  | ... |           | c1s2.png     ← カード先頭(デッキは継続)
             |              | s_0006           | チャプター1  | シーン2  | ... |           |              
...
d_002        | c_009        | s_0033           | チャプター2  | シーン1  | ... | c2.png    | c2s1.png     ← デッキが切り替わる
```

- 先頭行以外の行に画像ファイル名が書かれていた場合は**バリデーションエラー**
- 先頭行が空欄だった場合は「このデッキ/カードに画像なし」と解釈

### 1.6 ファイル名の命名規則

投稿者の自由(拡張子と S3 に許容される文字集合のみ強制)。

- 許容文字: 英数字、アンダースコア(`_`)、ハイフン(`-`)、ドット(`.`)
- 禁止: 空白、日本語、スラッシュ、その他特殊文字
- 音声: `.mp3` / `.wav` / `.m4a` 等(実装で定義)
- 画像: `.png` / `.jpg` / `.jpeg` / `.webp` 等
- 動画(将来): `.mp4` 等

同じファイル名を複数行(複数センテンス/複数カード)が参照しても良い。
その場合、S3上には1つのオブジェクトのみ存在し、DB上の複数レコードが
同じキーを参照する。

### 1.7 セル値の正規化と拡張属性列

#### 1.7.1 セル値の正規化(全列共通)

エクセルの全セル値は、パース時に次の正規化を**この順で**受ける
(ヘッダー行を含む全列共通):

1. **改行の置換**: セル内改行(`\r\n` / `\r` / `\n`)→ 半角スペース
2. **連続空白の圧縮**: 連続する空白 → 半角スペース1個。「空白」は
   Unicode 規則で判定され、**全角空白(U+3000)や NBSP(U+00A0)も
   対象に含まれる**(全角空白1個も半角スペース1個に置き換わる)
3. **前後トリム**: 値の先頭・末尾の空白を除去

この正規化の帰結として:

- **セル内改行・全角空白は保存されない**。複数行タイトル(英日2行併記
  など)の構造をセル内改行で表現することはできない。構造を持つ値は
  拡張属性列(§1.7.2)で運ぶこと
- 分かち書き(語順訳)の区切りに全角空白を用いた場合、区切りは**半角
  スペースに転写されて保存される**(区切り数=チャンク数は保たれる)。
  投稿形式(保存形)における `直訳`(語順訳)のチャンク区切りは**半角
  スペース**である。本正規化により**保存形に全角空白は存在し得ず**、
  これは既存の投稿済みデータについても同様である(全セル共通の正規化の
  ため)。消費側は半角スペース区切りを前提としてよい
- 階層の識別キー比較(§1.3)は、この正規化**後**の値で行われる
- 文字正規化(NFKC/NFC 等)は行わない。全角英数・半角カナはそのまま
  保存される

**セルの型**: セル値は表示書式適用後の文字列として読まれる。数値・日付
形式のセルは Excel の表示書式に依存した文字列になるため、**全セルを
文字列型で作成することを推奨**する(特に「セット1」のような数字を含む
タイトルをツールで生成する場合は文字列型で書き出すこと)。

#### 1.7.2 拡張属性列(カード属性 / デッキ属性)

列16「カード属性」・列17「デッキ属性」(§1.2)の規定:

- **記入粒度**: カード属性はカードの**先頭行のみ**、デッキ属性はデッキの
  **先頭行のみ**記入する(デッキ画像・カード画像 §1.5 と同じ規約)。
  先頭行以外への記入はバリデーションエラー(§4.1)
- **値**: JSON としてパースできる文字列。パース不能な値は
  バリデーションエラー(§4.1)。スキーマは定めない
- **長さ**: 上限は設けない(xlsx のセル上限 32,767 文字が事実上の上限と
  なる)。参考として、想定実需は数百バイト級(話者メタデータや多言語
  タイトル程度)。乱用対策としてのサイズ上限は、メディアのサイズ上限と
  同様にフェーズ2の検討事項とする(§10 残論点2)
- **無解釈の原則**: システムは JSON の中身を**解釈・検証・変形しない**。
  中身の意味論(スキーマ、記法規約)は、投稿データを生成する側と消費する
  アプリ側(教材ファミリー)の合意に属し、本仕様のスコープ外である
- **出力**: デッキ属性は `body` トップレベルの `extra` に、カード属性は
  `cards[].extra` に、**§1.7.1 正規化後の文字列をそのまま JSON 文字列値
  として**格納される(パース済みオブジェクトではない。§9.1 / §9.2)
- **往復保証**: UC-23 のダウンロードは、**保存済みの文字列をそのまま
  出力する(再シリアライズしない)**。§1.7.1 の正規化は正規化済み文字列に
  対して冪等であるため、DL したエクセルを無変更で再アップロードしても
  内容は変化しない。拡張属性列は先頭行のみ記入の形で出力される
  (DL エクセルにこの2列が含まれることが往復維持の前提である点に注意)
- **正規化との関係**: 拡張属性列も §1.7.1 の正規化を受ける。正当な JSON は
  文字列値内の改行・空白をエスケープ表記(`\n` `\u3000` 等)で表現する限り
  影響を受けない。生成ツールは **JSON をミニファイして出力**し、空白の
  忠実性が必要な文字列値にはエスケープ表記を用いること

---

## 2. メディアパック(zip構造)

本章は **メディアパックモード** を選択した場合の zip ファイル構造を
定める。自動生成モードを選択した場合は zip のアップロードは不要(§3
参照)。

メディアパックは **zip ファイル単位の一括差し替え** が基本方針である。
ユーザーが個別ファイル単位で差分処理を行う必要はない。

### 2.1 一括差し替えの原則

- 投稿時 / 更新時にアップロードされた zip ファイルが、ユニットの
  メディアの最新版となる
- 既存ファイルとの個別比較は行わない
  - 例: 64件中3件だけ差し替える場合でも、64件すべてを含む zip を再アップロードする
  - これにより整合性管理がシンプルになり、エラー時の復旧も容易
- 投稿者向けフロー:
  1. UC-23 で現在の zip をダウンロードして手元に保持
  2. 必要な編集を加えた新しい zip を作成
  3. 新しい zip を再アップロードして全置換

### 2.2 音声パック

```
audio.zip
├── c1_s1_u1.mp3
├── c1_s1_u2.mp3
├── c1_s1_u3.mp3
├── ...
└── c2_s8_u4.mp3
```

- フラット構造(サブディレクトリなし)を原則とする
- ファイル名はエクセルの「音声ファイル」列と完全一致
- zip内で重複するファイル名は不可

### 2.3 画像パック

```
images.zip
├── c1.png              ← デッキ画像
├── c2.png              ← デッキ画像
├── c1s1.png            ← カード画像
├── c1s2.png
├── ...
└── c2s8.png
```

- フラット構造を原則
- デッキ画像とカード画像は同一zip内に混在して良い
- ファイル名はエクセルの「デッキ画像」「カード画像」列と完全一致

### 2.4 動画の扱い(将来拡張)

画像パックに動画ファイルを含める形を想定するが、本フェーズでは対象外。
`画像パック.メディアタイプ : 静止画 / 動画` enum との対応は、将来
「画像パック」を「メディアパック」に概念拡張するタイミングで整備する。

---

## 3. メディア供給モード(自動生成 / メディアパック)の排他選択

ユニットの音声・画像メディアは、それぞれ独立に **自動生成モード** と
**メディアパックモード** のいずれかで供給する。両モードは **排他選択** で
あり、行ごとの混在は許可しない(ファイル名衝突や運用上の複雑さを
避けるため)。

### 3.1 モードの選択

A-MU3 新規投稿画面 / A-MU2 編集画面に以下のラジオを配置する:

- **音声メディア**:
  - メディアパック(zip)をアップロード
  - TTS で自動生成
- **画像メディア**:
  - メディアパック(zip)をアップロード
  - AI画像で自動生成

音声と画像は独立に選べる(音声=TTS / 画像=パック の組み合わせも可、
逆も可)。

### 3.2 メディアパックモードの挙動

- 投稿者はエクセルの該当列(音声ファイル / デッキ画像 / カード画像)を
  **すべて記入**し、対応する zip をアップロードする
- エクセルと zip の参照整合性は厳格にチェック(§4.2)
- ファイル名衝突や余剰ファイルはエラーで投稿拒否

### 3.3 自動生成モードの挙動

- 投稿者はエクセルの該当列を **空欄のままにする**(自動生成対象)
- システムが生成し、各ファイルにシステム生成名を付与する
  - 命名規則例: `_gen_sent_00001.mp3`, `_gen_deck_001.png`,
    `_gen_card_001.png` のような予約プレフィクス
  - 命名規則の正確な仕様は実装フェーズで詳細設計する(当面は
    予約プレフィクス方式で衝突回避を保証)
- 生成完了後、システムがエクセル上の該当列にもシステム生成名を
  反映する(UC-23 のダウンロード時に整合した状態で取得できる)
- **全件生成完了後、生成済みファイルをまとめて zip 化し、S3 の
  `packs/audio.zip` または `packs/images.zip` として保存する**
  (メディアパックモードと同じ配置)。これによりダウンロードは
  S3 から単一オブジェクトとして取得でき、署名付きURL方式とも整合する
- 部分完了の段階(N件中M件完了、M < N)ではまだ zip を作成しない。
  UC-23 のダウンロードボタンは disable にし、A-MU2 上で「生成中」
  表示にする(進捗バー、§3.6 / ワイヤーフレーム修正仕様参照)

#### TTSボイスの解決(話者 → ボイス)

TTS自動生成モードでは、各センテンスの音声を生成する際のボイスと
パラメータを、エクセルの「話者」列(`センテンス.話者`)の値から解決する。

**設定ファイル**: teachingmaterial 内の `/config/chatterboxtts.csv`

- 話者名 → ボイス + TTSパラメータの対応表。実体の管理・メンテナンスは
  teachingmaterial 側で行い、本システムはこのCSVを読み取って参照する。
  本仕様で定めるのは参照の契約のみで、CSVの中身は teachingmaterial の
  リポジトリで管理する
- CSVは UTF-8。先頭に BOM が付き得るため、パーサは BOM を除去してから
  ヘッダ行を読む(先頭列「話者」を正しく認識するため)

**CSVの列構成(7列)**:

| 列 | 意味 |
|---|---|
| 話者 | 話者名。エクセルの「話者」列と突き合わせるキー |
| voice | Chatterbox TTS のボイスID |
| lang | 言語コード(例: en) |
| exaggeration | 抑揚の強さ |
| cfg_weight | 生成の忠実度の重み |
| temperature | 生成のゆらぎ |
| seed | 乱数シード(固定すると生成が決定的になる) |

**解決アルゴリズム**(センテンスごとに評価):

1. 話者の値が CSV の「話者」列のいずれかと **完全一致**(大文字小文字を
   区別)する場合 → その行の voice + 各パラメータ(lang / exaggeration /
   cfg_weight / temperature / seed)を用いてTTS生成する
2. 話者が空欄、または CSV のどの話者名とも一致しない場合 →
   **カード内の順番の偶奇**でフォールバックする:
   - 各カードの中で奇数番目のセンテンス → `voice=alloy_female_cheerful_en`
   - 各カードの中で偶数番目のセンテンス → `voice=ash_male_cheerful_en`
   - いずれも `seed=12345` を指定し、`exaggeration` / `cfg_weight` /
     `temperature` / `lang` はTTSエンジンの既定値を用いる
     (CSVの Alloy / Ash 行が持つ値は使わない)

偶奇は **カードごとにリセット**する(各カードの先頭センテンスから
1, 2, 3 … と数えた順番=`センテンス.順番` の偶奇)。これにより2話者の
対話で、各カード(シーン)の先頭発話が必ず女声、2番目が男声、と安定
して交互になる。

**補足**:

- 「話者」列は **学習者向けの表示ラベル** と **ボイス解決キー** を
  兼ねる。`案内係` `旅行者` のような日本語ロール名は CSV にマッチせず
  全行フォールバックになる。特定のボイスを当てたい行だけ、CSV に
  登録済みの話者名(`Alloy` `Ash` 等)を記入することで上書きできる
- デッキタイプが「文」のデッキでも同じ偶奇フォールバックが適用される。
  単一ボイスにしたい場合は各行の話者欄に同一のCSV話者名を記入する
  (将来、文デッキを単一ボイス既定にする調整は残論点)
- この解決は **TTS(音声)自動生成モードのみ**に適用される。AI画像
  自動生成のプロンプト設計は別途扱う(本フェーズ残論点)

### 3.4 モードの混在(行レベルの混在)は不可

「同じユニット内で、音声のうち一部の行は zip 由来・一部は TTS 生成」
のような行レベル混在は **許可しない**。混在を許すとファイル名衝突の
リスクが生じ、UC-23 のダウンロードや UC-22 の更新時の処理が複雑化
するため。

ただし、**音声と画像は独立に選べる**ので、「音声=zip / 画像=AI画像」
のような組み合わせは可能。

### 3.5 モード切替(UC-22)

A-MU2 上でメディア供給モードを後から切り替えることができる。
切替時の挙動は以下:

| 切替前 | 切替後 | 挙動 |
|---|---|---|
| 自動生成 | メディアパック | 既存の生成物(展開ファイル + 旧 zip)を **全削除** → アップロードされた新 zip を `packs/` に保存し展開ファイルを配置 |
| メディアパック | 自動生成 | 既存の zip 原本と展開ファイルを **全削除** → 自動生成ジョブを投入 → **完了後に新 zip を `packs/` に作成** |
| 自動生成 | 自動生成(再生成) | 既存の生成物(展開ファイル + 旧 zip)を **全削除** → 自動生成ジョブを再投入 → **完了後に新 zip を `packs/` に作成** |
| メディアパック | メディアパック(差し替え) | 既存の zip 原本と展開ファイルを **全削除** → 新 zip を保存し展開ファイルを配置 |

いずれのケースも「**既存をすべて削除して入れ替える**」というシンプルな
原則で統一する。エクセルの該当列(音声ファイル / デッキ画像 / カード画像)
の値は、新モードに応じてシステムが書き換える(自動生成モードに切り替えた
場合は空欄に、生成完了後にシステム生成名に更新)。

**運用例**: 自動生成で本登録 → UC-23 で生成物をダウンロード → 手元で
気に入らないファイルを別途調達したファイルで差し替え → エクセルの
該当列も手元で編集 → A-MU2 でメディアパックモードに切り替え + 編集
済み zip と編集済みエクセルをアップロード → メディアパックモードで
更新完了。

### 3.6 自動生成完了のタイミング

自動生成は **A-MU3 での本登録(「投稿する」または「下書き保存」)の後**、
非同期ジョブで実行される(§6 参照)。投稿者はジョブ完了を A-MU2 編集
画面で待つ(進捗表示)。

サーバー側生成処理を伴うため、登録前プレビューは原理的に成立せず、
A-MU3 では登録前のプレビュー画面を提供しない。生成された音声・画像の
確認は A-MU2 で行う。

詳細は §6 / §7 の処理フローおよびユースケース仕様書「3. ユニット投稿」
セクション冒頭の基本方針を参照。

---

## 4. バリデーション

A-MU3 投稿時 / A-MU2 差分適用時に実施する検証。

### 4.1 構造チェック(エクセル単体)

- **ヘッダー行の検査**: 必須列(チャプター名、シーン名、英会話)の
  ヘッダーが存在するか(不存在はエラー)。列の同定はヘッダー名による
  (§1.2)ため、列の並び順は検査しない。**既知の列名に一致しない
  ヘッダーの列は読み飛ばす(エラー・警告は出さない)**。この帰結として、
  任意列(話者、語順訳、拡張属性列など)のヘッダー名の誤記は無警告の
  データ欠落となる点に注意(投稿後、UC-23 のダウンロードで反映結果を
  確認できる)
- 必須列(チャプター名、シーン名、英会話)が全て埋まっているか
- 編集ID列は無くてもよい(新規作成時)
- 編集ID列が存在する場合は、記入ルール(§1.4)に違反していないか
  - パート/デッキ/カード編集ID列が、先頭行以外の位置に書かれていないか
  - センテンス編集ID列以外の編集ID列が、先頭行で空欄でない場合に
    階層内のID整合性が取れているか(同一階層内で同一ID)
- デッキ画像・カード画像が先頭行以外に書かれていないか(§1.5)
- カード属性・デッキ属性が先頭行以外に書かれていないか(§1.7.2)
- カード属性・デッキ属性に記入がある場合、JSON としてパース可能か
  (§1.7.2。中身のスキーマは検証しない)
- ファイル名に禁止文字が含まれていないか(§1.6)

### 4.2 整合性チェック(エクセル × zip)

メディアパックモードのときに、**不整合はエラーとして投稿を拒否する**
(自動補完は行わない)。

不整合と判定されるケース:

1. エクセルで参照されているファイル名が zip にない
2. zip にあるがエクセルから参照されていない余剰ファイル
3. 同じファイル名が zip 内で重複
4. ファイル拡張子が許容リスト外

自動生成モードのときは、エクセルの該当列はすべて空欄でなければならない
(モード一貫性チェック、§4.5)。

エラー時の UI 表示例(既存ワイヤーフレームの警告バー):

> エラー:
> - 音声ファイル `c1_s1_u1.mp3` がエクセルで参照されていますが、zipに見つかりません
> - 画像ファイル `extra.png` がzipに含まれていますが、エクセルから参照されていません
> - デッキ「チャプター2」のカード画像がシーン先頭行ではない位置(行15)に記載されています

### 4.3 スキーマ制約チェック(UC-14 連携)

- 最大デッキ数、最大カード数、デッキ内最大カード数 を超過していないか
- 超過時はエラーで投稿拒否

### 4.4 編集ID の存在性チェック(UC-22 更新時)

- エクセルに記入されている編集IDが、当該ユニット配下に実在するか
- 他ユニットの編集IDが混入していないか
- 不正な編集ID参照はエラーとして拒否

### 4.5 モード一貫性チェック

メディア供給モード(自動生成 / メディアパック)とエクセル・zip の
内容が整合しているかを検証する。

| モード | 期待される状態 | 違反時の挙動 |
|---|---|---|
| 音声 = メディアパック | エクセルの「音声ファイル」列が全行記入されている / 音声zipがアップロードされている | エラー: 「音声メディアパックモードでは音声ファイル列を全行記入し、音声zipをアップロードしてください」 |
| 音声 = 自動生成 | エクセルの「音声ファイル」列が全行空欄 / 音声zipなし | エラー: 「音声自動生成モードでは音声ファイル列は空欄にし、音声zipはアップロードしないでください」 |
| 画像 = メディアパック | エクセルの「デッキ画像」「カード画像」列がデッキ・カード先頭行に記入されている / 画像zipがアップロードされている | エラー: 同上(画像版) |
| 画像 = 自動生成 | エクセルの「デッキ画像」「カード画像」列が全行空欄 / 画像zipなし | エラー: 同上(画像版) |

排他選択を強制することで、混在状態を防止する。

---

## 5. S3 格納規則

### 5.1 アップロード方式

クライアントから直接 S3 にアップロードするのではなく、
**アプリサーバー経由(プロキシ方式)** で S3 に転送する。

```
クライアント → アプリサーバー → S3
```

理由:

- バリデーション(§4)をサーバー側で確実に実行できる
- AWS 認証情報がクライアントに漏れない
- 実装がシンプル(単一の入口でログ/監視を集約)

将来パフォーマンス問題が顕在化した場合は、署名付きURLによる
クライアント直接アップロード方式への移行を検討する。

### 5.2 公開範囲(フェーズ別方針)

S3 の公開範囲はフェーズに応じて段階的に厳格化する。

- **フェーズ1(トピックメーカー社内ドッグフーディング)**:
  S3 バケットは**全公開**。メディアURLは固定の S3 公開URL で
  クライアントから直接参照可能
- **フェーズ2(他団体受け入れ可能化以降)**: **署名付きURL方式**
  に移行。メディア配信時に動的に署名付きURLを発行し、有効期限と
  閲覧権限のチェックを伴う形で配信する

詳細はユースケース仕様書「S3 データの公開範囲(フェーズ別方針)」を参照。
クライアント側でメディアURLをハードコードしない設計を推奨。

### 5.3 キー構造

```
s3://{bucket}/
  {ユーザーID}/
    {ユニットID}/
      {ファイル名}                   ← 展開後の個別ファイル(配信・参照用)
      packs/
        audio.zip                    ← メディアパックモードの場合の原本zip
        images.zip                   ← メディアパックモードの場合の原本zip
      _cover.{ext}                   ← ユニット表紙画像(予約名)
```

- `ユーザーID`: Account の安定識別子(ULID等)
- `ユニットID`: Unit の安定識別子
- ファイル名は、メディアパックモードではエクセルで指定されたものを
  そのまま使う。自動生成モードではシステム生成名(`_gen_...` 等の
  予約プレフィクス、§3.3 参照)

### 5.4 モード別の格納方針

**両モード共通**: S3 上のファイル配置構造は同じ。`{ユニットID}/` 直下に
配信・参照用の個別ファイルを置き、`{ユニットID}/packs/` 配下に
ダウンロード用の zip を置く。違いは「ファイル名(ユーザー命名 vs
システム生成名)」と「zip の作成タイミング(アップロード時 vs 自動生成
完了時)」のみ。

#### 5.4.1 メディアパックモードの場合

- アップロードされた **zip 原本** を `packs/audio.zip` または
  `packs/images.zip` として保存
- zip を **展開した個別ファイル** を `{ユニットID}/` 直下に配置
  (配信・参照用)
- ファイル名はエクセルで指定されたユーザー命名

#### 5.4.2 自動生成モードの場合

- システムが生成した個別ファイルを `{ユニットID}/` 直下に配置
  (配信・参照用)。ファイル名はシステム生成名(`_gen_...` 等の予約
  プレフィクス、§3.3 参照)
- **全件生成完了後**、生成済みファイルをまとめて zip 化し、
  `packs/audio.zip` または `packs/images.zip` として保存
- 部分完了の段階(M < N 件完了)では zip を作成しない。UC-23 の
  ダウンロードボタンは disable

#### 5.4.3 UC-23 ダウンロードの統一挙動

両モードで、UC-23 のメディアダウンロードは **S3 `packs/` 配下の zip を
そのまま返す**(動的生成なし)。

- メリット: S3 の単一オブジェクトを直接配信できる(署名付きURL方式とも
  整合、サーバー側で zip 化するコスト・遅延を避けられる)
- 自動生成モードでは、ジョブ完了まではダウンロード不可(ボタン disable
  + A-MU2 上で「生成中」表示)

概念モデルの `音声パック.download()` `画像パック.download()` メソッドは、
この zip を返す機能として実装される(モード不問)。

#### 5.4.4 モード切替時の整合性維持

§3.5 の表の通り、モード切替時はすべて **「既存をすべて削除して入れ替え」**
で処理する:

- 旧モードの個別ファイル群と `packs/` 配下の zip をすべて削除
- 新モードに応じて新しいファイルを配置(zip保存+展開、または自動生成
  ジョブ投入 → 全件完了後に zip 作成)

これにより、複数モードの中間状態が S3 上に残ることはない。

### 5.5 ユニットID の発行タイミング

- **A-MU3「投稿する」または「下書き保存」ボタン押下時**にユニットIDが
  確定する
- それまではS3への保存は発生しない
- A-MU3 では**エクセル + (必要なら)zip の一括アップロード**が必須
- 「投稿する」と「下書き保存」の違いは公開設定のみ:
  - 「投稿する」: 投稿者が選択した公開設定で本登録
  - 「下書き保存」: 公開設定を「非公開」固定で本登録
- いずれの場合も登録後ただちに自動生成ジョブが起動し、A-MU2 に遷移する
  (詳細は §6 を参照)

### 5.6 ユニット表紙画像

A-MU3/A-MU2 の UI 上で**エクセルとは別に**直接アップロードする
(現行ワイヤーフレームの「カバー画像」フィールド通り)。S3キーは:

```
s3://{bucket}/{ユーザーID}/{ユニットID}/_cover.{ext}
```

アンダースコア始まりの予約ファイル名を使い、投稿者が同名のファイルを
エクセルから参照していた場合は警告(ただしこの予約ファイル名は
投稿者には公開しないので、通常衝突しない)。

### 5.7 UC-22 差分更新時の挙動

- ユニットIDは既に確定しているので、S3キーは維持される
- **エクセルの差分処理**: 編集IDによって行単位で create / update を
  判定し、DB を更新する(個別行レベルでの差分処理)
- **メディアの処理**: モード別に以下のように扱う(§3.5 のモード切替
  ルールも参照)

  | 更新内容 | 挙動 |
  |---|---|
  | モード変更なし、zip 再アップロードなし(メディアパック) | メディアは現状維持 |
  | モード変更なし、zip 再アップロード(メディアパック → メディアパック) | 旧 zip と展開ファイルを全削除 → 新 zip 保存・展開 |
  | モード変更なし(自動生成 → 自動生成、再生成) | 既存生成物 + 旧 zip を全削除 → 自動生成ジョブを再投入 → 完了後に新 zip 作成 |
  | モード変更あり(自動生成 → メディアパック) | 既存生成物 + 旧 zip を全削除 → 新 zip 保存・展開 |
  | モード変更あり(メディアパック → 自動生成) | 旧 zip と展開ファイルを全削除 → 自動生成ジョブを投入 → 完了後に新 zip 作成 |

- 差分プレビューでこれらの変化を投稿者に表示する(モード変更時は
  特に強く警告)

---

## 6. 処理フロー(A-MU3 新規投稿)

投稿フローは「**本登録 → 自動生成 → A-MU2 でレビュー(必要なら修正)**」を
基本方針とする。サーバー側生成処理を伴うため、登録前のプレビュー画面は
設けない(生成物がまだ存在しないため、原理的に成立しない)。

メディア供給モードは音声・画像それぞれ独立に選択し、両モードは排他
(§3 参照)。以下のフローでは「メディアパックモード」と「自動生成モード」
を分岐させて記述する。

```
1. 投稿者がエクセルをアップロード
   ↓
2. 投稿者が音声・画像それぞれのメディア供給モードを選択
   - メディアパックモード: 対応するzipを同時にアップロード
   - 自動生成モード: zipはアップロードしない
   ↓
3. システムがエクセルをパース
   ↓
4. 構造チェック(§4.1)
   ↓ NG → エラー表示、中断
   ↓ OK
5. モード一貫性チェック(§4.5)
   ↓ NG → エラー表示、中断
   ↓ OK
6. zip がある場合は展開し、整合性チェック(§4.2)
   ↓ NG → エラー表示、中断
   ↓ OK
7. スキーマ制約チェック(§4.3)
   ↓ NG → エラー表示、中断
   ↓ OK
8. 投稿者が「投稿する」または「下書き保存」をクリック
   ↓
9. ユニットIDを発行
   ↓
10. エクセル内容をDBに永続化(編集IDを各レコードに付与)
    - ユニット → [パート] → デッキ → カード → センテンス
      (パートは任意。パート列が無ければデッキはブック直下に帰属)
    - 公開状態:
      - 「投稿する」: 投稿者が A-MU3 で選択した公開設定で保存
      - 「下書き保存」: 「非公開」固定で保存
    - メディア供給モード(音声・画像それぞれ)もユニット属性として保存
    ↓
11. メディア配置(モード別):
    - メディアパックモード: zip 原本を packs/ に保存 → 展開して個別
      ファイルを配置
    - 自動生成モード: 自動生成ジョブを非同期に投入
      (個別ファイルは生成完了時に配置される)
    ↓
12. A-MU2 に遷移(ユニット詳細/編集画面)
    ↓
13. (自動生成モードのみ・非同期)生成ジョブが順次実行され、
    個別ファイルが S3 に配置される
    ↓
14. (自動生成モードのみ・非同期)**全件生成完了後、生成済みファイルを
    まとめて zip 化し、packs/audio.zip または packs/images.zip として
    S3 に保存**(§3.3 / §5.4.2)
    ↓
15. 投稿者が A-MU2 上で:
    - 自動生成完了を待つ(進捗表示。該当モードの場合のみ)
    - 完了後にレビュー(再生・表示確認、必要なら修正)
    - 必要に応じて公開設定や料金などを編集して保存
    - メディア一括ダウンロードも可能になる(完了前は disable)
```

ステップ9〜12までが「本登録」のスコープ(投稿者から見るとここまでで
A-MU2 への遷移が起こる)。ステップ13〜14は自動生成モードのみの非同期
完了処理。投稿者から見ると、A-MU3 でボタンを押した後はそのまま A-MU2
に遷移してコンテンツが揃うのを待つ流れになる。

---

## 7. 処理フロー(A-MU2 差分更新)

```
1. A-MU2 画面で「⭳ 投稿データをダウンロード」クリック
   ↓
2. システムが現在のDB内容をエクセル形式で出力
   - 編集ID列に各レコードの編集IDを反映
   - 音声ファイル列、デッキ画像列、カード画像列は現在のS3キーを反映
     (自動生成モードの場合はシステム生成名が反映される)
   - カード属性列・デッキ属性列は保存済みの拡張属性を先頭行のみ記入の
     形で反映(§1.7.2 の往復保証)
   - **列12「チェック」は DB 非保存(§1.2)のため DL 出力に含まれない**。
     したがって標準 DL は最大**16列**(パート未使用ユニットは**14列**)と
     なる。チェック列の値は投稿者側の作業フラグであり、往復では保持され
     ない(必要なら手元のエクセルで管理する)
   - パート所属デッキとブック直下デッキが混在する場合、直下デッキは
     先頭の無題パートとして出力する(§1.3)。このとき A-MU2 上に
     「ブック直下のデッキは先頭の無題パートとして出力されます。
     再アップロードすると当該パートに帰属します」の旨のガイドを表示する
   ↓
3. (必要なら)A-MU2 上で「メディアをダウンロード」クリック
   - モードを問わず、S3 `packs/` 配下の zip を取得(§5.4.3)
   - 自動生成モードで生成中(zipまだ未作成)の場合はボタンが disable
   ↓
4. 投稿者がエクセル(+必要なら zip)を手元で編集
   - 編集ID列は触らない
   - メディア供給モードを変更したい場合は、A-MU2 上で次のステップで
     モードを切り替える
   ↓
5. A-MU2 でメディア供給モードを選択し、編集後エクセル(+必要なら新しい
   zip)をアップロード
   - 自動生成モード → メディアパックモード への切替時は、ダウンロード
     した zip を手元で編集し直して再アップロードするのが一般的な運用
   ↓
6. システムがエクセルの差分を解析
   - 編集ID列が空欄の行 → create
   - 編集ID列に値がある行 → update
   - DLされた行のうちアップロードに含まれない行 → delete(削除はWeb UI経由のみ、§後述)
   ↓
7. バリデーション(§4、モード一貫性チェック §4.5 を含む)
   ↓ NG → エラー、中断
   ↓ OK
8. 差分プレビュー表示
   - エクセル変更サマリ(例: create 3 / update 12 / ignore 50)
   - メディア操作サマリ(モード切替の有無、zip再アップロードの有無、
     自動生成ジョブの再投入予定など)。モード変更時は強く警告
   ↓
9. 投稿者が「差分を適用」クリック
   ↓
10. DB 更新(モード変更があればユニット属性も更新)
    ↓
11. メディア処理(§5.7 の表に従う):
    - モード変更なし、メディアパック zip 再アップロード: 旧 zip と展開
      ファイルを削除、新 zip を保存・展開
    - モード変更なし、自動生成(再生成): 既存生成物 + 旧 zip を削除、
      自動生成ジョブを再投入(全件完了後に新 zip を作成、§3.3 / §5.4.2)
    - モード変更あり(自動生成 → メディアパック): 既存生成物 + 旧 zip
      を削除、新 zip を保存・展開
    - モード変更あり(メディアパック → 自動生成): 旧 zip と展開ファイル
      を削除、自動生成ジョブを投入(全件完了後に新 zip を作成)
    - メディア処理なし(エクセルのみ更新): 何もしない
    ↓
12. 完了
```

> 注: 行単位の **削除** は本フローでは扱わない(エクセル経由ではなく
> A-MU2 の Web UI 上の操作で行う)。この原則は**データ行(デッキ / カード /
> センテンス)**に適用される。パートはデッキの帰属実態から決まるグルーピング層
> であり本原則の対象外 — 差分更新の結果デッキを全て失ったパートは削除される
> (§1.3)。詳細はワイヤーフレーム参照。

---

## 8. 既存データ例との対応

参考データ: 「インバウンド会話_higoアカウント_0422版.xlsx」

- **ユニット**: 「インバウンド会話」(ファイル名から類推)
- **パート**: なし(パート未使用。デッキはブック直下に帰属)
- **デッキ**: 2個(チャプター1:日本到着と街歩き、チャプター2:日本文化と旅の締めくくり)
- **カード**: 16個(各デッキ内に8シーン)
- **センテンス**: 64個(各カード内に4発話)

このデータに本仕様を適用すると、初回投稿時は編集ID列とパート列を省略し、
表示用列(チャプター名〜カード画像)のみで構成でき、以下のようになる:

```
チャプター | シーン  | 話者   | 英会話        | ... | チェック | 音声            | デッキ画像 | カード画像
チャプター1 | シーン1 | 案内係 | Hello, can... | ... | 0        | c1s1u1.mp3      | c1.png    | c1s1.png
チャプター1 | シーン1 | スタフ | Sure, please. | ... | 0        | c1s1u2.mp3      |           |
チャプター1 | シーン1 | 案内係 | Thank you, ...| ... | 0        | c1s1u3.mp3      |           |
チャプター1 | シーン1 | スタフ | You can buy...| ... | 0        | c1s1u4.mp3      |           |
チャプター1 | シーン2 | 旅行者 | Good after... | ... | 0        | c1s2u1.mp3      |           | c1s2.png
...
チャプター2 | シーン1 | 観光客 | Can you show..| ... | 0        | c2s1u1.mp3      | c2.png    | c2s1.png
...
```

投稿成功後、UC-23 で再ダウンロードすると、編集ID列(デッキ/カード/
センテンス編集ID。パート未使用のためパート編集IDは付かない)が先頭に
付与されたエクセルが取得できる。次回更新時はそれを編集してアップロードする。

zipに含まれるファイル:

- 音声zip: 64個のmp3(c1s1u1.mp3 〜 c2s8u4.mp3)
- 画像zip: 2個のデッキ画像(c1.png, c2.png)+ 16個のカード画像(c1s1.png 〜 c2s8.png)

---

## 9. 出力形式(デッキ body)

本章は、投稿されたエクセルデータからサーバー上に作成されるデッキ
オブジェクトの `body` 属性の出力形式を定める。

`body` は **JSONとしてパースできる文字列を保持する String 属性**である
(ORM の対象外。スキーマ変更・拡張が多くRDBに正規化しづらいため、オブジェクト
列ではなく文字列として保持する)。消費側はこの文字列をパースして使う。
`body` のJSONは `type` / `extra`(任意) / `cards` から成る(§9.1)。

デッキタイトル(`title`)、解析オブジェクト(`analysis`)、デッキ画像は
`body` の外、**デッキオブジェクトのプロパティ**として持つ(§9.3)。

`body` の構造(`cards` / `talks` / デッキタイプ)は、フラッシュカードアプリ
の動作モードと密結合している(動作モードはデッキタイプ等から決まる)。

### 9.1 body のJSON構造(トップレベル)

`body` 文字列をパースした JSON のトップレベル:

| フィールド | 内容 |
|---|---|
| `type` | デッキ種別。ミニブックは `"mini"` 固定 |
| `extra` | デッキの拡張属性(= `デッキ.拡張属性` = エクセルの「デッキ属性」列の値)。**JSON 文字列値**として格納され、パースは消費側で行う(下記)。**任意**: 記入がある場合のみ出力。システムは中身を解釈しない(§1.7.2) |
| `cards` | カードの配列(§9.2) |

**extra は文字列値**: `extra` の値は、拡張属性列の(§1.7.1 正規化後の)
文字列を**そのまま**保持した JSON 文字列値であり、パース済みオブジェクト
ではない。消費側は `body` をパースした後、`extra` を改めて JSON として
パースして用いる(二段パース)。原文文字列を保持するのは往復保証
(§1.7.2)のためである — オブジェクトとして格納するとダウンロード時に
再シリアライズが必要になり、エスケープ表記(`\u3000` 等)が実文字に
展開されて §1.7.1 の正規化と衝突し、往復で内容が変わり得る。

デッキ属性の出力先をデッキオブジェクトのプロパティ(§9.3)ではなく
`body` 内に置くのは、**デッキ内容の永続化と変更有無の判定(UC-22 の
update 適用要否)が `body` 全体を単位として行われる**ためである。
UC-22 における行の create / update の対応付け自体は編集IDで行う
(§1.4 / §7 手順6)。`body` 内の値は追加実装なしで永続化・変更判定の
対象になり、拡張のたびにデッキオブジェクトのスキーマと変更判定の
比較対象を増やさずに済む。

### 9.2 cards[] / talks[](ミニブック表示構造)

`cards[]` の各要素:

| フィールド | 内容 |
|---|---|
| `title` | シーンタイトル(= `カード.タイトル` = エクセルの「シーン名」列) |
| `image` | カード画像の**ファイル名**(= `カード.画像ファイルID` = エクセルの「カード画像」列)。§9.4 |
| `extra` | カードの拡張属性(= `カード.拡張属性` = エクセルの「カード属性」列の値)。**JSON 文字列値**(§9.1 の extra と同じ扱い: 原文文字列を保持、パースは消費側の二段パース)。**任意**: 記入がある場合のみ出力。システムは中身を解釈しない(§1.7.2) |
| `talks` | 発話の配列 |

`talks[]` の各要素:

| フィールド | 内容 |
|---|---|
| `person` | 話者(= `センテンス.話者` = エクセルの「話者」列) |
| `en` | 英文(= `センテンス.英文` = エクセルの「英会話」列) |
| `ja1` | 直訳・語順訳。分かち書き(= `センテンス.直訳` = エクセルの「語順訳」列) |
| `ja2` | 和訳(= `センテンス.和訳` = エクセルの「和訳」列) |
| `audio` | 音声の**ファイル名**(= `センテンス.音声ファイルID` = エクセルの「音声ファイル」列)。§9.4 |

**フィールドの欠落規定**: `talks[]` の各フィールド(`person` / `en` /
`ja1` / `ja2` / `audio`)は、エクセルの該当列が空欄・不存在でも**キー自体は
常に出力**され、値は空文字列となる。一方、`cards[].image` / `cards[].extra` /
`body.extra` は**記入がある場合のみキーが出力**される(欠落 = 値なし)。
消費側はこの差を前提にしてよい。

### 9.3 デッキオブジェクトのプロパティ(body 外)

以下は `body` 文字列の中ではなく、デッキオブジェクトのプロパティ
(`body` と同階層)として持つ。

| プロパティ | 内容 |
|---|---|
| `title` | デッキタイトル(= `デッキ.タイトル` = エクセルの「チャプター名」列) |
| `analysis` | 既存「ちょい英語」の解析オブジェクト(下記) |
| `image` | デッキ画像のファイル名(= `デッキ.画像ファイルID` = エクセルの「デッキ画像」列) |

`analysis` は既存「ちょい英語」が持つ解析オブジェクトをそのまま保持する
(内部構造は既存デッキシステム側の定義に従う)。デッキ全体の連結テキストに
対する文単位の言語解析(構文解析・難易度・辞書等)を保持し、
`センテンス.構文解析結果` 等に対応する。

重要な性質として、**`analysis` はデッキ単位のブロブであり、`body` の
`cards[].talks[]` とは1対1で対応しない**。解析器が1発話を複数の文に分割
することがあるため、`analysis` 内の文の件数は talks の総数と一致しない
ことがある(これは織り込み済みで、表示コンポーネントが対応する)。
したがって出力上、発話と解析を行番号で突き合わせる前提は置かない。

### 9.4 メディアの載せ方

- 音声・画像は **ファイル名** で格納し、完全URLは焼き込まない
- 配信パスは §5.3 のキー規約 `{ユーザーID}/{ユニットID}/{ファイル名}` から
  導出する。`ユーザーID` / `ユニットID` は配信文脈から既知のため、
  ファイル名だけからパスを組み立てられる
- フェーズ1は公開URL、フェーズ2は署名付きURL(§5.2)。完全URLを永続化
  しないため、署名URLの期限切れによる陳腐化が起きない
- **デッキ画像は `body` に含めない**。デッキ画像(= `デッキ.画像ファイルID`
  = エクセルの「デッキ画像」列)は、デッキオブジェクトのプロパティとして持つ(§9.3)
- デッキオブジェクトは **自動生成の全件完了後にのみ配信される**(未完了時は
  アクセス不可。§3.3 / §5.4.2 / A-MU2 の生成ゲートと一貫)。したがって
  配信時点で `talks[].audio` / `cards[].image` は常に有効なファイル名で
  埋まり、「未生成」を表す空値を出力に表現する必要はない

### 9.5 入力・モデル・出力の対応表

| エクセル列 | 概念モデル属性 | 出力フィールド | 出力先 |
|---|---|---|---|
| チャプター名 | `デッキ.タイトル` | `title` | デッキオブジェクト |
| シーン名 | `カード.タイトル` | `cards[].title` | body |
| 話者 | `センテンス.話者` | `talks[].person` | body |
| 英会話 | `センテンス.英文` | `talks[].en` | body |
| 語順訳 | `センテンス.直訳` | `talks[].ja1` | body |
| 和訳 | `センテンス.和訳` | `talks[].ja2` | body |
| 音声ファイル | `センテンス.音声ファイルID` | `talks[].audio`(ファイル名) | body |
| カード画像 | `カード.画像ファイルID` | `cards[].image`(ファイル名) | body |
| デッキ画像 | `デッキ.画像ファイルID` | `image`(ファイル名) | デッキオブジェクト |
| カード属性 | `カード.拡張属性` | `cards[].extra`(JSON文字列。無解釈 §1.7.2) | body |
| デッキ属性 | `デッキ.拡張属性` | `extra`(JSON文字列。無解釈 §1.7.2) | body(トップレベル) |
| (解析) | `センテンス.構文解析結果` 等 | `analysis`(デッキ単位・talks と非1対1) | デッキオブジェクト |

出力に現れないもの: 編集ID列(内部処理用)、「チェック」列(DB非保存)、
「パート」列(デッキのグルーピング層。`body` はデッキ単位のため現れない)、
`公開ID`(URL用。パート・デッキが持つが、`body` の talks/cards 構造には
現れない)。

### 9.6 スケルトン例

§8 のインバウンド会話データに本形式を適用した最小例(1カード・2発話に
省略、`analysis` は既存オブジェクトのため内部を省略)。デッキオブジェクトを
示す。`body` の値は可読性のためネスト表記しているが、実際にはこのJSONを
文字列化して `body`(String)に格納する:

```json
{
  "title": "チャプター1: 日本到着と街歩き",
  "image": "c1.png",
  "analysis": { "...": "既存「ちょい英語」の解析オブジェクト(構文解析・辞書・難易度等)" },
  "body": {
    "type": "mini",
    "cards": [
      {
        "title": "シーン1: 空港でのチェックイン",
        "image": "c1s1.png",
        "talks": [
          { "person": "案内係", "en": "Hello, can I help you?", "ja1": "こんにちは お手伝い できる か", "ja2": "こんにちは、お手伝いできますか。", "audio": "c1s1u1.mp3" },
          { "person": "スタッフ", "en": "Sure, please.", "ja1": "もちろん どうぞ", "ja2": "もちろんです、どうぞ。", "audio": "c1s1u2.mp3" }
        ]
      }
    ]
  }
}
```

拡張属性列(§1.7.2)を使うユニットでは、「デッキ属性」の値が
`body.extra` に、「カード属性」の値が `cards[].extra` に、**JSON 文字列値の
まま**現れる(§9.1。消費側は二段パースで用いる)。以下は拡張属性を使う
教材の抜粋例(値の中身は教材ファミリー側の規約であり、システムはキー名・
構造を関知しない):

```json
  "body": {
    "type": "mini",
    "extra": "{\"title_en\":\"English for Yoroshiku\",\"title_ja\":\"よろしく、英語なら？\"}",
    "cards": [
      {
        "title": "セット1",
        "image": "theme001.png",
        "extra": "{\"speakers\":{\"A\":\"28歳、女性、オフィス、新入社員\",\"B\":\"41歳、男性、オフィス、上司\"}}",
        "talks": [ { "person": "A", "en": "It's nice / to meet / you today.", "ja1": "それうれしい 会うこと あなたに今日", "ja2": "", "audio": "id001_a0.mp3" } ]
      }
    ]
  }
```

---

## 10. 残論点 / 将来拡張

1. **ユニット表紙画像のS3キー衝突対策**: `_cover.{ext}` の予約名について、投稿者への公開範囲と衝突検知の実装詳細
2. **音声/画像/拡張属性の上限サイズ**: 1ファイルあたりの最大サイズ、zipの最大サイズ、拡張属性(カード属性・デッキ属性)のサイズ上限(§1.7.2。フェーズ1は上限なし)
3. **S3オブジェクトのライフサイクル**(フェーズ2): 非公開ユニットのメディアへの署名付きURLの発行、期限管理
4. **自動生成の非同期処理**: 投稿時に自動生成が完了しない場合、ユーザにどう見せるか(「生成中」バッジ等)。A-MU2 上での進捗表示の仕組み
5. **動画パック**: 将来的な拡張として概念モデルの `画像パック` を `メディアパック` に拡張
6. **ファイル名の大文字小文字**: S3キーは大文字小文字を区別するが、エクセルでの入力揺れ(Windows感覚で小文字化しがち)への対応
7. **メディアパックのバージョン管理**: 投稿データ(エクセル)はバージョン管理しない方針(UC-22注記)。メディアパック(zip原本)も同様にバージョン管理せず、最新版のみ保持(`packs/audio.zip`, `packs/images.zip` の単一名で上書き)
